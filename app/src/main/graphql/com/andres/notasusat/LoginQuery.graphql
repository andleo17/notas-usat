query LoginQuery($nickname: String!, $password: String!) {
    login(nickname: $nickname, password: $password) {
        user{
            id
            nickname
            email
            name
            lastname
            birthDate
            photo
            genre
            state
            semesterId
            schoolId
        }
        token
    }
}