<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andres.notasusat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ui.presentation.SplashActivity"  android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.presentation.DetalleCursoActivity" />
        <activity android:name=".calcularnotas" />
        <activity
            android:name=".navigation"
            android:label="@string/title_activity_navigation" />
        <activity
            android:name=".NAVI"
            android:label="@string/title_activity_n_a_v_i" />
        <activity android:name=".ui.presentation.CursoActivity" />
        <activity android:name=".ui.presentation.LoginActivity">

        </activity>
        <activity android:name=".ui.presentation.SingUpActivity"></activity>
        <activity android:name=".ui.presentation.MainActivity" />
        <activity android:name=".ui.presentation.PerfilActivity" />
    </application>

</manifest>